services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-55432}:5432"
    environment:
      POSTGRES_USER: prompt_proxy
      POSTGRES_PASSWORD: prompt_proxy
      POSTGRES_DB: prompt_proxy
    volumes:
      - prompt_proxy_postgres:/var/lib/postgresql/data

volumes:
  prompt_proxy_postgres:
